all:
  children:
    bastion:
      hosts:
        bastion_host:
          ansible_host: 
    frontend:
      hosts:
        frontend_server:
          ansible_host: 
    backend:
      hosts:
        backend_server:
          ansible_host: 10.0.3.103